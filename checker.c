#include <stdio.h>
#include <stdlib.h>
#include "libft/libft.h"
#include "readline/readline.h"
#include "readline/history.h"
#include "parser.h"

int	corrector(char *line)
{
	int i;
	char str[200];

	i = 0;
	check = 0;
	str = //readline
	while(str[i] != NULL)
	{
		if(str[i] == '\"')
			quotes_corrector
		else(str[i] == '\''
	}

}
